
name: Update GitHub Contribution Stats

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # Runs every day at midnight UTC

jobs:
  update-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Grants the action write permissions to the repository
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run update script
        run: node scripts/scaffold-github-stats.mjs
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          GH_USERNAME: "adilmunawar"

      - name: Commit and push changes
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add src/components/GitHubStats.tsx
          git diff --staged --quiet || git commit -m "chore: update GitHub contribution stats"
          git push
